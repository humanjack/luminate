# Database
DATABASE_URL=sqlite:///../luminate.db

# Application
DEBUG=true
HOST=0.0.0.0
PORT=8000

# CORS
CORS_ORIGINS=["http://localhost:3000"]

# LLM (Optional - can also be configured via /api/settings)
# ANTHROPIC_API_KEY=your-api-key-here
